# CLI, SDK, and endpoint navigation expansion

Give developer tooling its own CLI & SDKs tab. Document the implemented local CLI lifecycle, shared profiles, governed shell commands, approvals, recovery, and evidence export. Add typed governed-call examples for the development TypeScript and Python SDKs, with explicit simulation labels and role/deployment prerequisites. Preserve existing guide URLs and distinguish source distribution from public package releases.

Replace the long, expanded endpoint sidebar with collapsed product groups containing collapsed resource groups. Keep HTTP conventions visible. Preserve every endpoint route and all 127 operations. Use native Mintlify nested groups; top-level groups cannot collapse. Keep the grouping reproducible by sharing navigation generation with the reference generator.

Validate navigation coverage and collapsed structure, all internal links, Mintlify build, SDK snippet types/signatures, and isolated CLI initialization. Do not start or reset a stack during docs checks. Publish the validated change to the docs repository and observe Mintlify's deployment result.
