---
title: "Submit an action for governed execution"
description: "POST /v1/executions \u2014 The Rail customer contract 0.1.1."
openapi: "/api-reference/openapi.json POST /v1/executions"
---

<Note>Contract 0.1.1. Confirm that this operation is enabled in your deployment. Example hosts are placeholders; this page does not send API requests.</Note>

**Required OAuth scope:** `execution:write`. Tenant, object, and domain authorization also apply.

Supply an explicit `Idempotency-Key`. See [idempotency and retries](/api-reference/idempotency).

This is a consequential operation. Credentials and OAuth scope alone do not authorize the effect; applicable authority and policy must also permit it.

**Service:** `execution`. Use its operator-provided base URL, or a gateway explicitly configured to route this operation. See [implementation and release status](/get-started/implementation-status).

## Implementation notes

HTTP acceptance starts an asynchronous workflow. Dispatch requires the enrolled worker, target, authority reservation, and current boundary checks. The implemented conditional gate handles an approval obligation; unsupported obligation combinations remain unavailable. Submission alone does not run an arbitrary callback or target.
