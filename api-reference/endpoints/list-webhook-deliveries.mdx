---
title: "List webhook delivery attempts"
description: "GET /v1/webhook-subscriptions/{subscription_id}/deliveries \u2014 The Rail customer contract 0.1.1."
openapi: "/api-reference/openapi.json GET /v1/webhook-subscriptions/{subscription_id}/deliveries"
---

<Note>Contract 0.1.1. Confirm that this operation is enabled in your deployment. Example hosts are placeholders; this page does not send API requests.</Note>

**Required OAuth scope:** `events:read`. Tenant, object, and domain authorization also apply.
